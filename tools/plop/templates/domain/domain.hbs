import type { {{pascalCase singularName}} } from "./{{kebabCase name}}.types.d.ts";

export const canUserManage{{pascalCase singularName}} = ({{camelCase singularName}}: {{pascalCase singularName}}, userId: string): boolean => {
  return {{camelCase singularName}}.userId === userId;
};
